<!--
  GENERATED by scripts/spec-gen/generate.mjs from the code itself.
  Do not edit by hand: run `node scripts/spec-gen/extract.mjs && node scripts/spec-gen/generate.mjs`.
-->

# Data model

Every table, what it is for, and the fields that carry meaning rather than plumbing.

Money and quantities are `Decimal`, never float: a pharmacy that rounds a
unit price in binary floating point will disagree with its own invoices.
Primary keys are UUIDs so a record can be created in one branch and
referenced in another without a central sequence.

Generated 2026-09-03 from the schema, the route table and the permission catalogue.

**114 tables, 28 enumerations.**

## Identity and access

### ApiKey — `api_keys`


Relations: `id` → String, `name` → String, `description` → String, `prefix` → String, `keyHash` → String, `scopes` → String, `branchId` → String, `rateLimit` → Int, `expiresAt` → DateTime, `isActive` → Boolean, `lastUsedAt` → DateTime, `usageCount` → Int, `revokedAt` → DateTime, `revokedById` → String, `createdById` → String, `createdAt` → DateTime, `updatedAt` → DateTime

Indexes and constraints: `@@index([isActive])`, `@@map("api_keys")`

### Permission — `permissions`


Relations: `id` → String, `module` → String, `resource` → String, `action` → PermissionAction, `code` → String, `roles` → RolePermission

Indexes and constraints: `@@unique([module, resource, action])`, `@@map("permissions")`

### Role — `roles`


Relations: `id` → String, `code` → String, `name` → String, `description` → String, `isSystem` → Boolean, `createdAt` → DateTime, `permissions` → RolePermission, `users` → UserRole

Indexes and constraints: `@@map("roles")`

### RolePermission — `role_permissions`


Relations: `roleId` → String, `permissionId` → String, `maxAmount` → Decimal, `productCategoryId` → String, `approvalLevel` → Int, `role` → Role, `permission` → Permission

Indexes and constraints: `@@id([roleId, permissionId])`, `@@map("role_permissions")`

### Session — `sessions`


Relations: `id` → String, `userId` → String, `refreshHash` → String, `userAgent` → String, `ipAddress` → String, `deviceLabel` → String, `expiresAt` → DateTime, `revokedAt` → DateTime, `createdAt` → DateTime, `lastSeenAt` → DateTime, `user` → User

Indexes and constraints: `@@index([userId])`, `@@map("sessions")`

### User — `users`


Relations: `id` → String, `email` → String, `username` → String, `phone` → String, `passwordHash` → String, `fullName` → String, `status` → UserStatus, `mfaEnabled` → Boolean, `mfaSecret` → String, `mustChangePassword` → Boolean, `passwordChangedAt` → DateTime, `lastLoginAt` → DateTime, `failedAttempts` → Int, `lockedUntil` → DateTime, `licenseNumber` → String, `homeBranchId` → String, `resetTokenHash` → String, `resetTokenExpires` → DateTime, `createdAt` → DateTime, `updatedAt` → DateTime, `roles` → UserRole, `sessions` → Session, `scopes` → UserScope

Indexes and constraints: `@@index([status])`, `@@map("users")`

### UserRole — `user_roles`


Relations: `userId` → String, `roleId` → String, `user` → User, `role` → Role

Indexes and constraints: `@@id([userId, roleId])`, `@@map("user_roles")`

### UserScope — `user_scopes`


Relations: `id` → String, `userId` → String, `branchId` → String, `warehouseId` → String, `user` → User

Indexes and constraints: `@@unique([userId, branchId, warehouseId])`, `@@index([userId])`, `@@map("user_scopes")`


## Organisation

### Branch — `branches`


Relations: `id` → String, `organizationId` → String, `businessUnitId` → String, `regionId` → String, `code` → String, `name` → String, `branchType` → String, `timezone` → String, `costCentre` → String, `isHeadOffice` → Boolean, `licenseNumber` → String, `phone` → String, `email` → String, `addressLine` → String, `city` → String, `latitude` → Float, `longitude` → Float, `isActive` → Boolean, `createdAt` → DateTime, `updatedAt` → DateTime, `organization` → Organization, `businessUnit` → BusinessUnit, `region` → Region, `warehouses` → Warehouse, `departments` → Department

Indexes and constraints: `@@index([organizationId])`, `@@index([businessUnitId])`, `@@index([regionId])`, `@@map("branches")`

### BusinessUnit — `business_units`


Relations: `id` → String, `organizationId` → String, `code` → String, `name` → String, `description` → String, `isActive` → Boolean, `createdAt` → DateTime, `updatedAt` → DateTime, `organization` → Organization, `regions` → Region, `branches` → Branch

Indexes and constraints: `@@unique([organizationId, code])`, `@@map("business_units")`

### Department — `departments`


Relations: `id` → String, `branchId` → String, `code` → String, `name` → String, `costCentre` → String, `managerId` → String, `isActive` → Boolean, `createdAt` → DateTime, `updatedAt` → DateTime, `branch` → Branch

Indexes and constraints: `@@unique([branchId, code])`, `@@map("departments")`

### Region — `regions`


Relations: `id` → String, `organizationId` → String, `businessUnitId` → String, `code` → String, `name` → String, `isActive` → Boolean, `createdAt` → DateTime, `updatedAt` → DateTime, `organization` → Organization, `businessUnit` → BusinessUnit, `branches` → Branch

Indexes and constraints: `@@unique([organizationId, code])`, `@@index([businessUnitId])`, `@@map("regions")`

### Warehouse — `warehouses`


Relations: `id` → String, `branchId` → String, `code` → String, `name` → String, `isColdRoom` → Boolean, `isQuarantine` → Boolean, `minTempC` → Decimal, `maxTempC` → Decimal, `isActive` → Boolean, `createdAt` → DateTime, `updatedAt` → DateTime, `branch` → Branch, `locations` → WarehouseLocation, `balances` → InventoryBalance, `sensors` → TemperatureSensor

Indexes and constraints: `@@index([branchId])`, `@@map("warehouses")`

### WarehouseLocation — `warehouse_locations`


Relations: `id` → String, `warehouseId` → String, `parentId` → String, `code` → String, `name` → String, `level` → String, `locationType` → String, `storageCondition` → StorageCondition, `capacityUnits` → Decimal, `maxWeightKg` → Decimal, `barcode` → String, `pickSequence` → Int, `isPickFace` → Boolean, `isActive` → Boolean, `createdAt` → DateTime, `updatedAt` → DateTime, `warehouse` → Warehouse, `parent` → WarehouseLocation, `children` → WarehouseLocation, `balances` → InventoryBalance, `tasksFrom` → WarehouseTask, `tasksTo` → WarehouseTask

Indexes and constraints: `@@unique([warehouseId, code])`, `@@index([parentId])`, `@@index([warehouseId, locationType])`, `@@map("warehouse_locations")`

### WarehouseTask — `warehouse_tasks`


Relations: `id` → String, `taskNo` → String, `warehouseId` → String, `branchId` → String, `taskType` → String, `status` → String, `priority` → Int, `productId` → String, `batchId` → String, `fromLocationId` → String, `toLocationId` → String, `suggestedLocationId` → String, `quantity` → Decimal, `quantityDone` → Decimal, `referenceType` → String, `referenceId` → String, `waveId` → String, `assignedToId` → String, `assignedAt` → DateTime, `startedAt` → DateTime, `completedAt` → DateTime, `completedById` → String, `notes` → String, `shortReason` → String, `createdById` → String, `createdAt` → DateTime, `updatedAt` → DateTime, `fromLocation` → WarehouseLocation, `toLocation` → WarehouseLocation, `wave` → PickWave

Indexes and constraints: `@@index([warehouseId, status])`, `@@index([assignedToId, status])`, `@@index([taskType, status])`, `@@index([waveId])`, `@@index([referenceType, referenceId])`, `@@map("warehouse_tasks")`


## Drug master

### Manufacturer — `manufacturers`


Relations: `id` → String, `name` → String, `country` → String, `isActive` → Boolean, `createdAt` → DateTime, `products` → Product

Indexes and constraints: `@@map("manufacturers")`

### Product — `products`


Relations: `id` → String, `sku` → String, `gtin` → String, `genericName` → String, `brandName` → String, `activeIngredient` → String, `strength` → String, `dosageForm` → String, `routeOfAdmin` → String, `manufacturerId` → String, `marketingAuthHolder` → String, `countryOfOrigin` → String, `registrationNumber` → String, `registrationExpiry` → DateTime, `launchDate` → DateTime, `discontinuedDate` → DateTime, `categoryId` → String, `therapeuticClass` → String, `atcCode` → String, `baseUnit` → String, `purchaseUnitId` → String, `dispensingUnitId` → String, `packSize` → Int, `requiresPrescription` → Boolean, `isControlled` → Boolean, `controlledSchedule` → String, `isNarcotic` → Boolean, `isCytotoxic` → Boolean, `isFrozen` → Boolean, `isFragile` → Boolean, `isFlammable` → Boolean, `isLookAlikeSoundAlike` → Boolean, `isVeterinary` → Boolean, `isPediatric` → Boolean, `saleClassification` → String, `pregnancyInfo` → String, `lactationInfo` → String, `minHumidityPercent` → Decimal, `maxHumidityPercent` → Decimal, `maxExcursionMinutes` → Int, `isColdChain` → Boolean, `isRefrigerated` → Boolean, `isHazardous` → Boolean, `isHighAlert` → Boolean, `lightSensitive` → Boolean, `humidityRestricted` → Boolean, `storageCondition` → StorageCondition, `minTempC` → Decimal, `maxTempC` → Decimal, `minShelfLifeDaysOnReceipt` → Int, `reorderLevel` → Decimal, `safetyStock` → Decimal, `maximumStock` → Decimal, `leadTimeDays` → Int, `preferredSupplierId` → String, `secondarySupplierId` → String, `economicOrderQty` → Decimal, `minPurchaseQty` → Decimal, `purchaseMultiple` → Decimal, `minSaleQty` → Decimal, `maxDispenseQty` → Decimal, `procurementRestricted` → Boolean, `seasonalProfile` → Json, `purchaseCost` → Decimal, `averageCost` → Decimal, `lastPurchaseCost` → Decimal, `retailPrice` → Decimal, `wholesalePrice` → Decimal, `insurancePrice` → Decimal, `taxRate` → Decimal, `targetMarginPct` → Decimal, `imageUrl` → String, `patientInfoUrl` → String, `notes` → String, `isActive` → Boolean, `createdAt` → DateTime, `updatedAt` → DateTime, `createdById` → String, `manufacturer` → Manufacturer, `category` → ProductCategory, `units` → ProductUnit, `barcodes` → ProductBarcode, `batches` → Batch, `balances` → InventoryBalance, `transactions` → InventoryTransaction, `priceHistory` → PriceHistory, `supplierLinks` → SupplierProduct, `ingredients` → ProductIngredient, `attributes` → ProductAttribute, `relations` → ProductRelation, `relatedTo` → ProductRelation, `priceListItems` → PriceListItem

Indexes and constraints: `@@index([genericName])`, `@@index([atcCode])`, `@@index([categoryId])`, `@@index([registrationExpiry])`, `@@index([brandName])`, `@@index([isControlled])`, `@@index([isActive])`, `@@map("products")`

### ProductAttribute — `product_attributes`


Relations: `id` → String, `productId` → String, `definitionId` → String, `value` → String, `updatedAt` → DateTime, `product` → Product, `definition` → AttributeDefinition

Indexes and constraints: `@@unique([productId, definitionId])`, `@@map("product_attributes")`

### ProductBarcode — `product_barcodes`


Relations: `id` → String, `productId` → String, `barcode` → String, `symbology` → String, `unitCode` → String, `isPrimary` → Boolean, `product` → Product

Indexes and constraints: `@@unique([barcode, symbology])`, `@@index([productId])`, `@@map("product_barcodes")`

### ProductCategory — `product_categories`


Relations: `id` → String, `code` → String, `name` → String, `parentId` → String, `parent` → ProductCategory, `children` → ProductCategory, `products` → Product

Indexes and constraints: `@@map("product_categories")`

### ProductIngredient — `product_ingredients`


Relations: `id` → String, `productId` → String, `name` → String, `strengthValue` → Decimal, `strengthUnit` → String, `role` → String, `sequence` → Int, `product` → Product

Indexes and constraints: `@@unique([productId, name, role])`, `@@index([name])`, `@@map("product_ingredients")`

### ProductRelation — `product_relations`


Relations: `id` → String, `productId` → String, `relatedProductId` → String, `relationType` → String, `notes` → String, `createdById` → String, `createdAt` → DateTime, `product` → Product, `relatedProduct` → Product

Indexes and constraints: `@@unique([productId, relatedProductId, relationType])`, `@@index([relatedProductId])`, `@@map("product_relations")`

### ProductUnit — `product_units`


Relations: `id` → String, `productId` → String, `code` → String, `name` → String, `factorToBase` → Decimal, `isBaseUnit` → Boolean, `isPurchaseUnit` → Boolean, `isDispenseUnit` → Boolean, `product` → Product

Indexes and constraints: `@@unique([productId, code])`, `@@map("product_units")`


## Stock

### Batch — `batches`


Relations: `id` → String, `batchNumber` → String, `lotNumber` → String, `productId` → String, `supplierId` → String, `manufacturerName` → String, `manufacturingDate` → DateTime, `expiryDate` → DateTime, `receivedDate` → DateTime, `receivedQuantity` → Decimal, `purchaseCost` → Decimal, `status` → BatchStatus, `quarantineReason` → QuarantineReason, `qualityNotes` → String, `releasedById` → String, `releasedAt` → DateTime, `supplierInvoiceNo` → String, `purchaseOrderId` → String, `goodsReceiptId` → String, `parentBatchId` → String, `createdAt` → DateTime, `updatedAt` → DateTime, `product` → Product, `supplier` → Supplier, `parentBatch` → Batch, `childBatches` → Batch, `balances` → InventoryBalance, `transactions` → InventoryTransaction, `serials` → SerialNumber, `recallLinks` → RecallBatch

Indexes and constraints: `@@unique([productId, batchNumber])`, `@@index([expiryDate])`, `@@index([status])`, `@@index([productId, status, expiryDate]) // FEFO lookup path (§8)`, `@@map("batches")`

### CostConsumption — `cost_consumptions`


Relations: `id` → String, `layerId` → String, `transactionId` → String, `productId` → String, `quantity` → Decimal, `unitCost` → Decimal, `totalCost` → Decimal, `createdAt` → DateTime

Indexes and constraints: `@@index([transactionId])`, `@@index([layerId])`, `@@map("cost_consumptions")`

### CostLayer — `cost_layers`


Relations: `id` → String, `productId` → String, `batchId` → String, `warehouseId` → String, `transactionId` → String, `receivedAt` → DateTime, `quantity` → Decimal, `remainingQuantity` → Decimal, `unitCost` → Decimal, `createdAt` → DateTime

Indexes and constraints: `@@index([productId, warehouseId, receivedAt])`, `@@index([batchId])`, `@@map("cost_layers")`

### InventoryBalance — `inventory_balances`


Relations: `id` → String, `productId` → String, `batchId` → String, `warehouseId` → String, `locationId` → String, `branchId` → String, `onHand` → Decimal, `reserved` → Decimal, `lastMovementAt` → DateTime, `updatedAt` → DateTime, `product` → Product, `batch` → Batch, `warehouse` → Warehouse, `location` → WarehouseLocation

Indexes and constraints: `@@unique([productId, batchId, warehouseId, locationId])`, `@@index([warehouseId])`, `@@index([branchId])`, `@@index([productId])`, `@@map("inventory_balances")`

### InventoryTransaction — `inventory_transactions`


Relations: `id` → String, `occurredAt` → DateTime, `type` → TransactionType, `productId` → String, `batchId` → String, `serialId` → String, `warehouseId` → String, `locationId` → String, `branchId` → String, `quantityIn` → Decimal, `quantityOut` → Decimal, `balanceAfter` → Decimal, `unitCost` → Decimal, `referenceType` → String, `referenceId` → String, `referenceNo` → String, `idempotencyKey` → String, `reason` → String, `performedById` → String, `createdAt` → DateTime, `product` → Product, `batch` → Batch

Indexes and constraints: `@@index([productId, warehouseId, occurredAt])`, `@@index([batchId])`, `@@index([referenceType, referenceId])`, `@@index([occurredAt])`, `@@map("inventory_transactions")`

### SerialEvent — `serial_events`


Relations: `id` → String, `serialId` → String, `eventType` → String, `fromStatus` → String, `toStatus` → String, `referenceType` → String, `referenceId` → String, `referenceNo` → String, `warehouseId` → String, `branchId` → String, `performedById` → String, `reason` → String, `occurredAt` → DateTime, `serial` → SerialNumber

Indexes and constraints: `@@index([serialId, occurredAt])`, `@@index([eventType])`, `@@map("serial_events")`

### SerialNumber — `serial_numbers`


Relations: `id` → String, `batchId` → String, `serial` → String, `status` → String, `warehouseId` → String, `lastReferenceType` → String, `lastReferenceId` → String, `lastMovedAt` → DateTime, `createdAt` → DateTime, `updatedAt` → DateTime, `batch` → Batch, `events` → SerialEvent

Indexes and constraints: `@@unique([batchId, serial])`, `@@index([serial])`, `@@index([status])`, `@@map("serial_numbers")`

### StockAdjustment — `stock_adjustments`


Relations: `id` → String, `adjustmentNo` → String, `warehouseId` → String, `branchId` → String, `status` → DocumentStatus, `reason` → String, `createdById` → String, `approvedById` → String, `approvedAt` → DateTime, `createdAt` → DateTime, `items` → StockAdjustmentItem

Indexes and constraints: `@@map("stock_adjustments")`

### StockAdjustmentItem — `stock_adjustment_items`


Relations: `id` → String, `adjustmentId` → String, `productId` → String, `batchId` → String, `quantityDelta` → Decimal, `lossType` → String, `reason` → String, `adjustment` → StockAdjustment

Indexes and constraints: `@@map("stock_adjustment_items")`

### StockCount — `stock_counts`


Relations: `id` → String, `countNo` → String, `warehouseId` → String, `branchId` → String, `countType` → CountType, `isBlind` → Boolean, `isFrozen` → Boolean, `frozenAt` → DateTime, `status` → DocumentStatus, `scheduledAt` → DateTime, `startedAt` → DateTime, `completedAt` → DateTime, `countedById` → String, `approvedById` → String, `createdAt` → DateTime, `items` → StockCountItem

Indexes and constraints: `@@map("stock_counts")`

### StockCountItem — `stock_count_items`


Relations: `id` → String, `stockCountId` → String, `productId` → String, `batchId` → String, `locationId` → String, `systemQty` → Decimal, `countedQty` → Decimal, `varianceQty` → Decimal, `varianceValue` → Decimal, `requiresApproval` → Boolean, `reason` → String, `stockCount` → StockCount

Indexes and constraints: `@@map("stock_count_items")`

### StockReservation — `stock_reservations`


Relations: `id` → String, `productId` → String, `batchId` → String, `warehouseId` → String, `balanceId` → String, `quantity` → Decimal, `referenceType` → String, `referenceId` → String, `expiresAt` → DateTime, `releasedAt` → DateTime, `createdById` → String, `createdAt` → DateTime

Indexes and constraints: `@@index([referenceType, referenceId])`, `@@index([batchId])`, `@@map("stock_reservations")`

### StockTransfer — `stock_transfers`


Relations: `id` → String, `transferNo` → String, `fromWarehouseId` → String, `toWarehouseId` → String, `fromBranchId` → String, `toBranchId` → String, `status` → TransferStatus, `reason` → String, `requestedById` → String, `approvedById` → String, `dispatchedById` → String, `receivedById` → String, `vehicleOrCourier` → String, `driverName` → String, `driverPhone` → String, `trackingNumber` → String, `expectedArrival` → DateTime, `dispatchedAt` → DateTime, `receivedAt` → DateTime, `isRecallMovement` → Boolean, `createdAt` → DateTime, `updatedAt` → DateTime, `items` → StockTransferItem

Indexes and constraints: `@@index([status])`, `@@map("stock_transfers")`

### StockTransferItem — `stock_transfer_items`


Relations: `id` → String, `transferId` → String, `productId` → String, `batchId` → String, `requestedQty` → Decimal, `dispatchedQty` → Decimal, `receivedQty` → Decimal, `varianceReason` → String, `transfer` → StockTransfer

Indexes and constraints: `@@map("stock_transfer_items")`


## Procurement

### GoodsReceipt — `goods_receipts`


Relations: `id` → String, `grnNo` → String, `purchaseOrderId` → String, `supplierId` → String, `warehouseId` → String, `branchId` → String, `supplierInvoiceNo` → String, `receivedAt` → DateTime, `status` → DocumentStatus, `receivedById` → String, `inspectedById` → String, `notes` → String, `createdAt` → DateTime, `purchaseOrder` → PurchaseOrder, `items` → GoodsReceiptItem

Indexes and constraints: `@@map("goods_receipts")`

### GoodsReceiptItem — `goods_receipt_items`


Relations: `id` → String, `goodsReceiptId` → String, `productId` → String, `batchId` → String, `batchNumber` → String, `expiryDate` → DateTime, `manufacturingDate` → DateTime, `receivedQty` → Decimal, `acceptedQty` → Decimal, `rejectedQty` → Decimal, `unitCost` → Decimal, `locationId` → String, `flags` → String, `rejectionReason` → String, `goodsReceipt` → GoodsReceipt

Indexes and constraints: `@@map("goods_receipt_items")`

### PurchaseOrder — `purchase_orders`


Relations: `id` → String, `poNo` → String, `supplierId` → String, `branchId` → String, `warehouseId` → String, `quotationId` → String, `status` → PurchaseOrderStatus, `orderDate` → DateTime, `expectedDate` → DateTime, `currency` → String, `subtotal` → Decimal, `taxTotal` → Decimal, `freightCost` → Decimal, `discountTotal` → Decimal, `grandTotal` → Decimal, `notes` → String, `createdById` → String, `approvedById` → String, `approvedAt` → DateTime, `createdAt` → DateTime, `updatedAt` → DateTime, `supplier` → Supplier, `items` → PurchaseOrderItem, `receipts` → GoodsReceipt

Indexes and constraints: `@@index([status])`, `@@index([supplierId])`, `@@map("purchase_orders")`

### PurchaseOrderItem — `purchase_order_items`


Relations: `id` → String, `purchaseOrderId` → String, `productId` → String, `orderedQty` → Decimal, `receivedQty` → Decimal, `unitPrice` → Decimal, `taxRate` → Decimal, `discountPct` → Decimal, `lineTotal` → Decimal, `purchaseOrder` → PurchaseOrder

Indexes and constraints: `@@map("purchase_order_items")`

### PurchaseRequest — `purchase_requests`


Relations: `id` → String, `requestNo` → String, `branchId` → String, `status` → DocumentStatus, `requestedById` → String, `department` → String, `reason` → String, `requiredBy` → DateTime, `approvedById` → String, `approvedAt` → DateTime, `rejectionReason` → String, `createdAt` → DateTime, `updatedAt` → DateTime, `items` → PurchaseRequestItem, `rfqs` → Rfq

Indexes and constraints: `@@index([status])`, `@@map("purchase_requests")`

### PurchaseRequestItem — `purchase_request_items`


Relations: `id` → String, `purchaseRequestId` → String, `productId` → String, `requestedQty` → Decimal, `currentStock` → Decimal, `reorderLevel` → Decimal, `forecastDemand` → Decimal, `notes` → String, `request` → PurchaseRequest

Indexes and constraints: `@@map("purchase_request_items")`

### Rfq — `rfqs`


Relations: `id` → String, `rfqNo` → String, `purchaseRequestId` → String, `status` → DocumentStatus, `issuedAt` → DateTime, `closesAt` → DateTime, `createdById` → String, `createdAt` → DateTime, `request` → PurchaseRequest, `items` → RfqItem, `quotations` → SupplierQuotation

Indexes and constraints: `@@map("rfqs")`

### RfqItem — `rfq_items`


Relations: `id` → String, `rfqId` → String, `productId` → String, `quantity` → Decimal, `rfq` → Rfq

Indexes and constraints: `@@map("rfq_items")`

### Supplier — `suppliers`


Relations: `id` → String, `code` → String, `companyName` → String, `contactName` → String, `phone` → String, `email` → String, `addressLine` → String, `city` → String, `country` → String, `taxId` → String, `licenseNumber` → String, `licenseExpiry` → DateTime, `paymentTerms` → String, `currency` → String, `leadTimeDays` → Int, `minimumOrderValue` → Decimal, `bankName` → String, `bankAccount` → String, `isActive` → Boolean, `isApproved` → Boolean, `riskLevel` → String, `riskNotes` → String, `creditLimit` → Decimal, `onTimeDeliveryRate` → Decimal, `avgLeadTimeDays` → Decimal, `rejectionRate` → Decimal, `shortShipmentRate` → Decimal, `qualityIncidents` → Int, `returnRate` → Decimal, `supplierScore` → Decimal, `createdAt` → DateTime, `updatedAt` → DateTime, `products` → SupplierProduct, `purchaseOrders` → PurchaseOrder, `quotations` → SupplierQuotation, `batches` → Batch, `invoices` → SupplierInvoice

Indexes and constraints: `@@map("suppliers")`

### SupplierInvoice — `supplier_invoices`


Relations: `id` → String, `internalNo` → String, `supplierInvoiceNo` → String, `supplierId` → String, `purchaseOrderId` → String, `goodsReceiptId` → String, `branchId` → String, `invoiceDate` → DateTime, `dueDate` → DateTime, `currency` → String, `subtotal` → Decimal, `taxTotal` → Decimal, `freightCost` → Decimal, `grandTotal` → Decimal, `amountPaid` → Decimal, `status` → SupplierInvoiceStatus, `matchStatus` → InvoiceMatchStatus, `matchNotes` → String, `disputeReason` → String, `createdById` → String, `approvedById` → String, `approvedAt` → DateTime, `createdAt` → DateTime, `updatedAt` → DateTime, `supplier` → Supplier, `items` → SupplierInvoiceItem, `payments` → SupplierPayment

Indexes and constraints: `@@unique([supplierId, supplierInvoiceNo])`, `@@index([status])`, `@@map("supplier_invoices")`

### SupplierInvoiceItem — `supplier_invoice_items`


Relations: `id` → String, `invoiceId` → String, `productId` → String, `quantity` → Decimal, `unitPrice` → Decimal, `taxRate` → Decimal, `lineTotal` → Decimal, `orderedQty` → Decimal, `receivedQty` → Decimal, `poUnitPrice` → Decimal, `variance` → String, `invoice` → SupplierInvoice

Indexes and constraints: `@@map("supplier_invoice_items")`

### SupplierPayment — `supplier_payments`


Relations: `id` → String, `paymentNo` → String, `invoiceId` → String, `amount` → Decimal, `method` → PaymentMethod, `reference` → String, `paidAt` → DateTime, `recordedById` → String, `notes` → String, `invoice` → SupplierInvoice

Indexes and constraints: `@@index([invoiceId])`, `@@map("supplier_payments")`

### SupplierProduct — `supplier_products`


Relations: `id` → String, `supplierId` → String, `productId` → String, `supplierSku` → String, `unitPrice` → Decimal, `moq` → Decimal, `leadTimeDays` → Int, `isPreferred` → Boolean, `supplier` → Supplier, `product` → Product

Indexes and constraints: `@@unique([supplierId, productId])`, `@@map("supplier_products")`

### SupplierQuotation — `supplier_quotations`


Relations: `id` → String, `quotationNo` → String, `rfqId` → String, `supplierId` → String, `validUntil` → DateTime, `currency` → String, `freightCost` → Decimal, `discountPct` → Decimal, `paymentTerms` → String, `deliveryDays` → Int, `landedCost` → Decimal, `score` → Decimal, `isSelected` → Boolean, `createdAt` → DateTime, `rfq` → Rfq, `supplier` → Supplier, `items` → SupplierQuotationItem

Indexes and constraints: `@@unique([rfqId, supplierId])`, `@@map("supplier_quotations")`

### SupplierQuotationItem — `supplier_quotation_items`


Relations: `id` → String, `quotationId` → String, `productId` → String, `unitPrice` → Decimal, `taxRate` → Decimal, `discountPct` → Decimal, `moq` → Decimal, `offeredShelfLifeDays` → Int, `quotation` → SupplierQuotation

Indexes and constraints: `@@map("supplier_quotation_items")`


## Sales and dispensing

### CashSession — `cash_sessions`


Relations: `id` → String, `sessionNo` → String, `branchId` → String, `cashierId` → String, `openedAt` → DateTime, `closedAt` → DateTime, `openingCash` → Decimal, `cashSales` → Decimal, `refunds` → Decimal, `cashExpenses` → Decimal, `expectedCash` → Decimal, `actualCash` → Decimal, `variance` → Decimal, `varianceReason` → String, `isOpen` → Boolean, `sales` → Sale

Indexes and constraints: `@@index([branchId, isOpen])`, `@@map("cash_sessions")`

### CustomerGroup — `customer_groups`


Relations: `id` → String, `code` → String, `name` → String, `description` → String, `discountPercent` → Decimal, `isActive` → Boolean, `createdAt` → DateTime, `updatedAt` → DateTime, `priceLists` → PriceList, `patients` → Patient

Indexes and constraints: `@@map("customer_groups")`

### Dispensing — `dispensings`


Relations: `id` → String, `dispensingNo` → String, `prescriptionId` → String, `patientId` → String, `branchId` → String, `warehouseId` → String, `pharmacistId` → String, `dispensedAt` → DateTime, `notes` → String, `createdAt` → DateTime, `prescription` → Prescription, `items` → DispensingItem

Indexes and constraints: `@@index([patientId])`, `@@map("dispensings")`

### DispensingItem — `dispensing_items`


Relations: `id` → String, `dispensingId` → String, `productId` → String, `batchId` → String, `quantity` → Decimal, `unitPrice` → Decimal, `fefoRecommendedBatchId` → String, `overrideReason` → String, `overrideById` → String, `dispensing` → Dispensing

Indexes and constraints: `@@map("dispensing_items")`

### Patient — `patients`


Relations: `id` → String, `patientCode` → String, `fullName` → String, `dateOfBirth` → DateTime, `sex` → String, `phone` → String, `addressLine` → String, `city` → String, `emergencyContactName` → String, `emergencyContactPhone` → String, `allergies` → String, `notes` → String, `patientType` → String, `organizationName` → String, `customerGroupId` → String, `preferredLanguage` → String, `communicationPrefs` → Json, `email` → String, `insuranceProvider` → String, `insuranceMemberNo` → String, `employerName` → String, `creditLimit` → Decimal, `creditBalance` → Decimal, `loyaltyPoints` → Int, `loyaltyTier` → String, `isAnonymized` → Boolean, `anonymizedAt` → DateTime, `anonymizedById` → String, `anonymizedReason` → String, `mergedAt` → DateTime, `mergedById` → String, `mergedIntoId` → String, `isActive` → Boolean, `createdAt` → DateTime, `updatedAt` → DateTime, `prescriptions` → Prescription, `sales` → Sale, `consents` → PatientConsent, `customerGroup` → CustomerGroup

Indexes and constraints: `@@index([fullName])`, `@@index([phone])`, `@@index([customerGroupId])`, `@@index([mergedIntoId])`, `@@map("patients")`

### PatientConsent — `patient_consents`


Relations: `id` → String, `patientId` → String, `consentType` → String, `version` → String, `granted` → Boolean, `grantedAt` → DateTime, `withdrawnAt` → DateTime, `method` → String, `recordedById` → String, `notes` → String, `patient` → Patient

Indexes and constraints: `@@index([patientId, consentType])`, `@@map("patient_consents")`

### Payment — `payments`


Relations: `id` → String, `saleId` → String, `method` → PaymentMethod, `amount` → Decimal, `reference` → String, `receivedAt` → DateTime, `sale` → Sale

Indexes and constraints: `@@map("payments")`

### Prescription — `prescriptions`


Relations: `id` → String, `prescriptionNo` → String, `patientId` → String, `branchId` → String, `prescriberName` → String, `prescriberLicense` → String, `facilityName` → String, `prescriptionDate` → DateTime, `status` → PrescriptionStatus, `reviewedById` → String, `reviewedAt` → DateTime, `rejectionReason` → String, `documentUrl` → String, `refillsAllowed` → Int, `refillsUsed` → Int, `createdAt` → DateTime, `updatedAt` → DateTime, `patient` → Patient, `items` → PrescriptionItem, `dispensings` → Dispensing

Indexes and constraints: `@@index([status])`, `@@index([patientId])`, `@@map("prescriptions")`

### PrescriptionItem — `prescription_items`


Relations: `id` → String, `prescriptionId` → String, `productId` → String, `strength` → String, `dosage` → String, `frequency` → String, `durationDays` → Int, `prescribedQty` → Decimal, `dispensedQty` → Decimal, `instructions` → String, `prescription` → Prescription

Indexes and constraints: `@@map("prescription_items")`

### Sale — `sales`


Relations: `id` → String, `saleNo` → String, `branchId` → String, `warehouseId` → String, `cashSessionId` → String, `patientId` → String, `prescriptionId` → String, `cashierId` → String, `status` → SaleStatus, `subtotal` → Decimal, `discountTotal` → Decimal, `taxTotal` → Decimal, `grandTotal` → Decimal, `costTotal` → Decimal, `soldAt` → DateTime, `voidedAt` → DateTime, `voidReason` → String, `createdAt` → DateTime, `updatedAt` → DateTime, `cashSession` → CashSession, `patient` → Patient, `items` → SaleItem, `payments` → Payment

Indexes and constraints: `@@index([branchId, soldAt])`, `@@index([status])`, `@@map("sales")`

### SaleItem — `sale_items`


Relations: `id` → String, `saleId` → String, `productId` → String, `batchId` → String, `quantity` → Decimal, `unitPrice` → Decimal, `unitCost` → Decimal, `discountPct` → Decimal, `taxRate` → Decimal, `lineTotal` → Decimal, `fefoRecommendedBatchId` → String, `overrideReason` → String, `sale` → Sale

Indexes and constraints: `@@map("sale_items")`


## Quality and compliance

### ControlledRegisterEntry — `controlled_register_entries`


Relations: `id` → String, `entryNo` → Int, `productId` → String, `batchId` → String, `branchId` → String, `entryType` → String, `quantityIn` → Decimal, `quantityOut` → Decimal, `runningBalance` → Decimal, `prescriptionId` → String, `prescriberName` → String, `patientId` → String, `performedById` → String, `witnessedById` → String, `reversalOfId` → String, `reversalReason` → String, `occurredAt` → DateTime, `createdAt` → DateTime

Indexes and constraints: `@@index([productId, branchId, occurredAt])`, `@@map("controlled_register_entries")`

### DamageReport — `damage_reports`


Relations: `id` → String, `reportNo` → String, `productId` → String, `batchId` → String, `warehouseId` → String, `branchId` → String, `quantity` → Decimal, `unitCost` → Decimal, `totalValue` → Decimal, `reason` → String, `damageType` → String, `status` → DamageStatus, `discoveredAt` → DateTime, `reportedById` → String, `verifiedById` → String, `verifiedAt` → DateTime, `verificationNotes` → String, `rejectionReason` → String, `disposalId` → String, `incidentId` → String, `createdAt` → DateTime

Indexes and constraints: `@@index([status])`, `@@index([batchId])`, `@@map("damage_reports")`

### Disposal — `disposals`


Relations: `id` → String, `disposalNo` → String, `branchId` → String, `warehouseId` → String, `status` → DocumentStatus, `method` → DisposalMethod, `reason` → String, `approvedById` → String, `approvedAt` → DateTime, `disposedAt` → DateTime, `witnessName` → String, `certificateNo` → String, `certificateUrl` → String, `totalCostValue` → Decimal, `createdById` → String, `createdAt` → DateTime, `items` → DisposalItem

Indexes and constraints: `@@map("disposals")`

### DisposalItem — `disposal_items`


Relations: `id` → String, `disposalId` → String, `productId` → String, `batchId` → String, `quantity` → Decimal, `unitCost` → Decimal, `reason` → String, `disposal` → Disposal

Indexes and constraints: `@@map("disposal_items")`

### QualityIncident — `quality_incidents`


Relations: `id` → String, `incidentNo` → String, `type` → QualityIncidentType, `status` → QualityIncidentStatus, `title` → String, `severity` → String, `sourceType` → String, `sourceId` → String, `productId` → String, `batchId` → String, `supplierId` → String, `branchId` → String, `description` → String, `rootCause` → String, `correctiveAction` → String, `preventiveAction` → String, `verification` → String, `reportedById` → String, `assignedToId` → String, `closedAt` → DateTime, `createdAt` → DateTime, `updatedAt` → DateTime

Indexes and constraints: `@@index([status])`, `@@index([sourceType, sourceId])`, `@@map("quality_incidents")`

### Recall — `recalls`


Relations: `id` → String, `recallNo` → String, `productId` → String, `manufacturerName` → String, `severity` → RecallSeverity, `status` → RecallStatus, `recallDate` → DateTime, `reason` → String, `regulatoryReference` → String, `instructions` → String, `serialRangeFrom` → String, `serialRangeTo` → String, `closedAt` → DateTime, `createdById` → String, `createdAt` → DateTime, `batches` → RecallBatch, `tasks` → RecallTask

Indexes and constraints: `@@map("recalls")`

### RecallBatch — `recall_batches`


Relations: `id` → String, `recallId` → String, `batchId` → String, `quantityInStockAtActivation` → Decimal, `quantityDispensedHistorical` → Decimal, `quantityRecovered` → Decimal, `quantityReturned` → Decimal, `quantityDestroyed` → Decimal, `previousBatchStatus` → BatchStatus, `recall` → Recall, `batch` → Batch

Indexes and constraints: `@@unique([recallId, batchId])`, `@@map("recall_batches")`

### RecallTask — `recall_tasks`


Relations: `id` → String, `recallId` → String, `batchId` → String, `branchId` → String, `warehouseId` → String, `taskType` → String, `patientId` → String, `dispensingId` → String, `quantity` → Decimal, `quantityRecovered` → Decimal, `status` → RecallTaskStatus, `assignedToId` → String, `notes` → String, `completedAt` → DateTime, `createdAt` → DateTime, `recall` → Recall

Indexes and constraints: `@@index([recallId, status])`, `@@map("recall_tasks")`

### ReturnDocument — `returns`


Relations: `id` → String, `returnNo` → String, `type` → ReturnType, `branchId` → String, `warehouseId` → String, `saleId` → String, `dispensingId` → String, `purchaseOrderId` → String, `supplierId` → String, `patientId` → String, `status` → DocumentStatus, `reason` → String, `createdById` → String, `inspectedById` → String, `inspectedAt` → DateTime, `createdAt` → DateTime, `items` → ReturnItem

Indexes and constraints: `@@map("returns")`

### ReturnItem — `return_items`


Relations: `id` → String, `returnId` → String, `productId` → String, `batchId` → String, `quantity` → Decimal, `condition` → String, `disposition` → ReturnDisposition, `dispositionNotes` → String, `returnDocument` → ReturnDocument

Indexes and constraints: `@@map("return_items")`

### SensorCalibration — `sensor_calibrations`


Relations: `id` → String, `sensorId` → String, `calibratedAt` → DateTime, `validUntil` → DateTime, `certificateNo` → String, `performedBy` → String, `referenceTempC` → Decimal, `measuredTempC` → Decimal, `result` → String, `notes` → String, `recordedById` → String, `createdAt` → DateTime, `sensor` → TemperatureSensor

Indexes and constraints: `@@index([sensorId, calibratedAt])`, `@@map("sensor_calibrations")`

### SensorMaintenance — `sensor_maintenance`


Relations: `id` → String, `sensorId` → String, `workType` → String, `performedAt` → DateTime, `performedBy` → String, `description` → String, `nextDueAt` → DateTime, `tookOffline` → Boolean, `offlineFrom` → DateTime, `offlineUntil` → DateTime, `recordedById` → String, `createdAt` → DateTime, `sensor` → TemperatureSensor

Indexes and constraints: `@@index([sensorId, performedAt])`, `@@map("sensor_maintenance")`

### TemperatureExcursion — `temperature_excursions`


Relations: `id` → String, `excursionNo` → String, `sensorId` → String, `startedAt` → DateTime, `endedAt` → DateTime, `durationMinutes` → Int, `minTempC` → Decimal, `maxTempC` → Decimal, `affectedBatchIds` → String, `affectedQuantity` → Decimal, `investigation` → String, `correctiveAction` → String, `disposition` → ExcursionDisposition, `decidedById` → String, `decidedAt` → DateTime, `createdAt` → DateTime, `sensor` → TemperatureSensor

Indexes and constraints: `@@index([disposition])`, `@@map("temperature_excursions")`

### TemperatureLog — `temperature_logs`


Relations: `id` → String, `sensorId` → String, `temperature` → Decimal, `humidity` → Decimal, `recordedAt` → DateTime, `isBreach` → Boolean, `sensor` → TemperatureSensor

Indexes and constraints: `@@index([sensorId, recordedAt])`, `@@map("temperature_logs")`

### TemperatureSensor — `temperature_sensors`


Relations: `id` → String, `code` → String, `name` → String, `warehouseId` → String, `locationId` → String, `minTempC` → Decimal, `maxTempC` → Decimal, `maxExcursionMinutes` → Int, `isActive` → Boolean, `lastReadingAt` → DateTime, `createdAt` → DateTime, `lastCalibratedAt` → DateTime, `calibrationDueAt` → DateTime, `calibrationInterval` → Int, `lastMaintenanceAt` → DateTime, `nextMaintenanceAt` → DateTime, `warehouse` → Warehouse, `logs` → TemperatureLog, `excursions` → TemperatureExcursion, `calibrations` → SensorCalibration, `maintenance` → SensorMaintenance

Indexes and constraints: `@@map("temperature_sensors")`


## Finance

### Account — `accounts`


Relations: `id` → String, `code` → String, `name` → String, `type` → String, `subType` → String, `parentId` → String, `currency` → String, `systemKey` → String, `isSystem` → Boolean, `isActive` → Boolean, `description` → String, `createdAt` → DateTime, `updatedAt` → DateTime, `parent` → Account, `children` → Account, `lines` → JournalLine

Indexes and constraints: `@@index([type])`, `@@index([parentId])`, `@@map("accounts")`

### AccountingPeriod — `accounting_periods`


Relations: `id` → String, `code` → String, `startDate` → DateTime, `endDate` → DateTime, `status` → String, `closedById` → String, `closedAt` → DateTime, `createdAt` → DateTime

Indexes and constraints: `@@index([startDate, endDate])`, `@@map("accounting_periods")`

### FinanceNote — `finance_notes`


Relations: `id` → String, `noteNo` → String, `noteType` → String, `direction` → String, `supplierId` → String, `patientId` → String, `branchId` → String, `referenceType` → String, `referenceId` → String, `noteDate` → DateTime, `currency` → String, `subtotal` → Decimal, `taxTotal` → Decimal, `grandTotal` → Decimal, `status` → String, `reason` → String, `createdById` → String, `approvedById` → String, `approvedAt` → DateTime, `createdAt` → DateTime, `updatedAt` → DateTime, `lines` → FinanceNoteLine

Indexes and constraints: `@@index([direction, status])`, `@@index([supplierId])`, `@@map("finance_notes")`

### FinanceNoteLine — `finance_note_lines`


Relations: `id` → String, `noteId` → String, `productId` → String, `description` → String, `quantity` → Decimal, `unitPrice` → Decimal, `taxRate` → Decimal, `lineTotal` → Decimal, `note` → FinanceNote

Indexes and constraints: `@@index([noteId])`, `@@map("finance_note_lines")`

### JournalEntry — `journal_entries`


Relations: `id` → String, `entryNo` → String, `entryDate` → DateTime, `description` → String, `sourceType` → String, `sourceId` → String, `branchId` → String, `departmentId` → String, `currency` → String, `status` → String, `totalDebit` → Decimal, `totalCredit` → Decimal, `reversalOfId` → String, `reversedById` → String, `postedById` → String, `postedAt` → DateTime, `createdAt` → DateTime, `lines` → JournalLine, `reversalOf` → JournalEntry, `reversals` → JournalEntry

Indexes and constraints: `@@unique([sourceType, sourceId])`, `@@index([entryDate])`, `@@index([status])`, `@@map("journal_entries")`

### JournalLine — `journal_lines`


Relations: `id` → String, `entryId` → String, `accountId` → String, `debit` → Decimal, `credit` → Decimal, `description` → String, `branchId` → String, `departmentId` → String, `productId` → String, `batchId` → String, `lineNumber` → Int, `entry` → JournalEntry, `account` → Account

Indexes and constraints: `@@index([entryId])`, `@@index([accountId])`, `@@map("journal_lines")`


## Platform

### AuditLog — `audit_logs`


Relations: `id` → String, `sequence` → Int, `userId` → String, `userLabel` → String, `ipAddress` → String, `userAgent` → String, `module` → String, `action` → String, `entityType` → String, `entityId` → String, `previousValue` → Json, `newValue` → Json, `reason` → String, `branchId` → String, `previousHash` → String, `hash` → String, `createdAt` → DateTime

Indexes and constraints: `@@index([entityType, entityId])`, `@@index([userId, createdAt])`, `@@index([module, action])`, `@@map("audit_logs")`

### AutomationEscalation — `automation_escalations`


Relations: `id` → String, `ruleId` → String, `subjectType` → String, `subjectId` → String, `level` → Int, `firstActedAt` → DateTime, `lastActedAt` → DateTime, `nextDueAt` → DateTime, `status` → String, `resolvedAt` → DateTime, `notes` → String, `rule` → AutomationRule

Indexes and constraints: `@@unique([ruleId, subjectType, subjectId])`, `@@index([status, nextDueAt])`, `@@map("automation_escalations")`

### AutomationRule — `automation_rules`


Relations: `id` → String, `code` → String, `name` → String, `description` → String, `triggerType` → String, `conditions` → Json, `actions` → Json, `escalations` → Json, `branchId` → String, `isActive` → Boolean, `priority` → Int, `cooldownHours` → Int, `isSystem` → Boolean, `lastRunAt` → DateTime, `lastMatchCount` → Int, `createdById` → String, `createdAt` → DateTime, `updatedAt` → DateTime, `runs` → AutomationRun, `escalationQueue` → AutomationEscalation

Indexes and constraints: `@@index([triggerType, isActive])`, `@@map("automation_rules")`

### AutomationRun — `automation_runs`


Relations: `id` → String, `ruleId` → String, `startedAt` → DateTime, `finishedAt` → DateTime, `durationMs` → Int, `subjectsScanned` → Int, `matched` → Int, `actionsRun` → Int, `suppressed` → Int, `failed` → Int, `trigger` → String, `errorMessage` → String, `sample` → Json, `rule` → AutomationRule

Indexes and constraints: `@@index([ruleId, startedAt])`, `@@map("automation_runs")`

### BackupRecord — `backup_records`


Relations: `id` → String, `fileName` → String, `sizeBytes` → BigInt, `status` → String, `encrypted` → Boolean, `checksum` → String, `verifiedAt` → DateTime, `errorMessage` → String, `startedAt` → DateTime, `completedAt` → DateTime

Indexes and constraints: `@@index([startedAt])`, `@@map("backup_records")`

### Document — `documents`


Relations: `id` → String, `entityType` → String, `entityId` → String, `fileName` → String, `mimeType` → String, `sizeBytes` → Int, `storageKey` → String, `expiresAt` → DateTime, `uploadedById` → String, `createdAt` → DateTime

Indexes and constraints: `@@index([entityType, entityId])`, `@@map("documents")`

### ImportBatch — `import_batches`


Relations: `id` → String, `reference` → String, `entityType` → String, `fileName` → String, `status` → String, `mapping` → Json, `delimiter` → String, `totalRows` → Int, `validRows` → Int, `errorRows` → Int, `importedRows` → Int, `failedRows` → Int, `rolledBackRows` → Int, `fileErrors` → Json, `errorSummary` → Json, `createdById` → String, `validatedAt` → DateTime, `importedAt` → DateTime, `rolledBackAt` → DateTime, `rollbackReason` → String, `createdAt` → DateTime, `updatedAt` → DateTime, `rows` → ImportRow

Indexes and constraints: `@@index([entityType, status])`, `@@map("import_batches")`

### ImportRow — `import_rows`


Relations: `id` → String, `batchId` → String, `rowNumber` → Int, `raw` → Json, `mapped` → Json, `status` → String, `errors` → Json, `entityId` → String, `action` → String, `batch` → ImportBatch

Indexes and constraints: `@@unique([batchId, rowNumber])`, `@@index([batchId, status])`, `@@map("import_rows")`

### IntegrationDelivery — `integration_deliveries`


Relations: `id` → String, `endpointId` → String, `event` → String, `payload` → Json, `status` → String, `attempts` → Int, `responseStatus` → Int, `lastError` → String, `nextAttemptAt` → DateTime, `deliveredAt` → DateTime, `createdAt` → DateTime, `endpoint` → IntegrationEndpoint

Indexes and constraints: `@@index([status, nextAttemptAt])`, `@@index([endpointId])`, `@@map("integration_deliveries")`

### IntegrationEndpoint — `integration_endpoints`


Relations: `id` → String, `name` → String, `url` → String, `description` → String, `events` → String, `secret` → String, `headers` → Json, `isActive` → Boolean, `consecutiveFailures` → Int, `lastDeliveryAt` → DateTime, `createdById` → String, `createdAt` → DateTime, `updatedAt` → DateTime, `deliveries` → IntegrationDelivery

Indexes and constraints: `@@map("integration_endpoints")`

### JobRun — `job_runs`


Relations: `id` → String, `jobKey` → String, `trigger` → String, `status` → String, `startedAt` → DateTime, `finishedAt` → DateTime, `durationMs` → Int, `result` → Json, `errorMessage` → String, `triggeredById` → String

Indexes and constraints: `@@index([jobKey, startedAt])`, `@@index([status])`, `@@map("job_runs")`

### Notification — `notifications`


Relations: `id` → String, `userId` → String, `roleCode` → String, `branchId` → String, `channel` → NotificationChannel, `severity` → NotificationSeverity, `eventType` → String, `title` → String, `body` → String, `linkUrl` → String, `payload` → Json, `readAt` → DateTime, `sentAt` → DateTime, `deliveryError` → String, `providerRef` → String, `deliveryAttempts` → Int, `createdAt` → DateTime

Indexes and constraints: `@@index([userId, readAt])`, `@@index([eventType])`, `@@index([channel, sentAt])`, `@@map("notifications")`

### NotificationRule — `notification_rules`


Relations: `id` → String, `eventType` → String, `channels` → NotificationChannel, `roleCodes` → String, `branchId` → String, `threshold` → Json, `isActive` → Boolean, `createdAt` → DateTime

Indexes and constraints: `@@unique([eventType, branchId])`, `@@map("notification_rules")`

### SavedReport — `saved_reports`


Relations: `id` → String, `name` → String, `description` → String, `dataSource` → String, `columns` → String, `filters` → Json, `groupBy` → String, `sort` → Json, `aggregates` → Json, `visualization` → String, `isShared` → Boolean, `schedule` → String, `recipients` → String, `lastRunAt` → DateTime, `ownerId` → String, `createdAt` → DateTime, `updatedAt` → DateTime

Indexes and constraints: `@@index([ownerId])`, `@@map("saved_reports")`

### WorkflowDefinition — `workflow_definitions`


Relations: `id` → String, `code` → String, `name` → String, `documentType` → String, `steps` → Json, `isActive` → Boolean, `createdAt` → DateTime, `instances` → WorkflowInstance

Indexes and constraints: `@@map("workflow_definitions")`

### WorkflowInstance — `workflow_instances`


Relations: `id` → String, `definitionId` → String, `documentType` → String, `documentId` → String, `currentStep` → Int, `status` → DocumentStatus, `amount` → Decimal, `branchId` → String, `createdAt` → DateTime, `completedAt` → DateTime, `definition` → WorkflowDefinition, `actions` → ApprovalAction

Indexes and constraints: `@@unique([documentType, documentId])`, `@@map("workflow_instances")`


## Other

### ApprovalAction — `approval_actions`


Relations: `id` → String, `instanceId` → String, `step` → Int, `action` → String, `actorId` → String, `comment` → String, `createdAt` → DateTime, `instance` → WorkflowInstance

Indexes and constraints: `@@map("approval_actions")`

### AttributeDefinition — `attribute_definitions`


Relations: `id` → String, `code` → String, `label` → String, `dataType` → String, `options` → String, `isRequired` → Boolean, `group` → String, `sequence` → Int, `isActive` → Boolean, `createdAt` → DateTime, `values` → ProductAttribute

Indexes and constraints: `@@map("attribute_definitions")`

### Dock — `docks`


Relations: `id` → String, `warehouseId` → String, `code` → String, `name` → String, `direction` → String, `isActive` → Boolean, `createdAt` → DateTime

Indexes and constraints: `@@unique([warehouseId, code])`, `@@map("docks")`

### FhirExchange — `fhir_exchanges`


Relations: `id` → String, `direction` → String, `resourceType` → String, `externalId` → String, `internalId` → String, `fhirVersion` → String, `operation` → String, `status` → String, `issues` → Json, `requestBody` → Json, `responseBody` → Json, `idempotencyKey` → String, `attempts` → Int, `errorMessage` → String, `apiKeyId` → String, `userId` → String, `durationMs` → Int, `createdAt` → DateTime

Indexes and constraints: `@@unique([idempotencyKey])`, `@@index([resourceType, status])`, `@@index([createdAt])`, `@@map("fhir_exchanges")`

### IdempotencyKey — `idempotency_keys`


Relations: `key` → String, `scope` → String, `resultId` → String, `createdAt` → DateTime

Indexes and constraints: `@@index([createdAt])`, `@@map("idempotency_keys")`

### LoginAttempt — `login_attempts`


Relations: `id` → String, `identifier` → String, `successful` → Boolean, `ipAddress` → String, `userAgent` → String, `reason` → String, `createdAt` → DateTime

Indexes and constraints: `@@index([identifier, createdAt])`, `@@map("login_attempts")`

### Organization — `organizations`


Relations: `id` → String, `name` → String, `legalName` → String, `taxId` → String, `licenseNumber` → String, `country` → String, `currency` → String, `timezone` → String, `dateFormat` → String, `defaultLocale` → String, `logoUrl` → String, `addressLine` → String, `city` → String, `phone` → String, `email` → String, `valuationMethod` → ValuationMethod, `allowNegativeStock` → Boolean, `createdAt` → DateTime, `updatedAt` → DateTime, `branches` → Branch, `settings` → SystemSetting, `businessUnits` → BusinessUnit, `regions` → Region

Indexes and constraints: `@@map("organizations")`

### PickWave — `pick_waves`


Relations: `id` → String, `waveNo` → String, `warehouseId` → String, `strategy` → String, `status` → String, `zoneId` → String, `releasedAt` → DateTime, `completedAt` → DateTime, `createdById` → String, `createdAt` → DateTime, `updatedAt` → DateTime, `tasks` → WarehouseTask, `packages` → ShipmentPackage

Indexes and constraints: `@@index([warehouseId, status])`, `@@map("pick_waves")`

### PriceHistory — `price_history`


Relations: `id` → String, `productId` → String, `branchId` → String, `priceType` → String, `oldValue` → Decimal, `newValue` → Decimal, `reason` → String, `effectiveFrom` → DateTime, `changedById` → String, `createdAt` → DateTime, `product` → Product

Indexes and constraints: `@@index([productId, createdAt])`, `@@map("price_history")`

### PriceList — `price_lists`


Relations: `id` → String, `code` → String, `name` → String, `listType` → String, `currency` → String, `branchId` → String, `customerGroupId` → String, `priority` → Int, `effectiveFrom` → DateTime, `effectiveTo` → DateTime, `isActive` → Boolean, `notes` → String, `createdById` → String, `createdAt` → DateTime, `updatedAt` → DateTime, `customerGroup` → CustomerGroup, `items` → PriceListItem

Indexes and constraints: `@@index([listType, isActive])`, `@@index([branchId])`, `@@map("price_lists")`

### PriceListItem — `price_list_items`


Relations: `id` → String, `priceListId` → String, `productId` → String, `unitPrice` → Decimal, `minQuantity` → Decimal, `effectiveFrom` → DateTime, `effectiveTo` → DateTime, `priceList` → PriceList, `product` → Product

Indexes and constraints: `@@unique([priceListId, productId, minQuantity])`, `@@index([productId])`, `@@map("price_list_items")`

### ShipmentPackage — `shipment_packages`


Relations: `id` → String, `packageNo` → String, `warehouseId` → String, `waveId` → String, `referenceType` → String, `referenceId` → String, `status` → String, `weightKg` → Decimal, `sealNumber` → String, `stagingLocationId` → String, `dockId` → String, `packedById` → String, `packedAt` → DateTime, `verifiedById` → String, `verifiedAt` → DateTime, `dispatchedAt` → DateTime, `departureTempC` → Decimal, `notes` → String, `createdAt` → DateTime, `updatedAt` → DateTime, `wave` → PickWave, `lines` → ShipmentPackageLine

Indexes and constraints: `@@index([warehouseId, status])`, `@@index([referenceType, referenceId])`, `@@map("shipment_packages")`

### ShipmentPackageLine — `shipment_package_lines`


Relations: `id` → String, `packageId` → String, `productId` → String, `batchId` → String, `quantity` → Decimal, `verifiedQuantity` → Decimal, `package` → ShipmentPackage

Indexes and constraints: `@@index([packageId])`, `@@map("shipment_package_lines")`

### SystemSetting — `system_settings`


Relations: `id` → String, `organizationId` → String, `key` → String, `value` → Json, `updatedById` → String, `updatedAt` → DateTime, `organization` → Organization

Indexes and constraints: `@@unique([organizationId, key])`, `@@map("system_settings")`


## Enumerations

- **BatchStatus**: `AVAILABLE`, `QUARANTINED`, `RELEASED`, `BLOCKED`, `DAMAGED`, `EXPIRED`, `RECALLED`, `RETURNED`, `DESTROYED`
- **CountType**: `FULL`, `CYCLE`, `CATEGORY`, `WAREHOUSE`, `BIN`, `RANDOM`
- **DamageStatus**: `REPORTED`, `VERIFIED`, `REJECTED`, `DISPOSED`
- **DisposalMethod**: `INCINERATION`, `RETURN_TO_SUPPLIER`, `LICENSED_WASTE_CONTRACTOR`, `ENCAPSULATION`, `LANDFILL_AUTHORIZED`
- **DocumentStatus**: `DRAFT`, `SUBMITTED`, `UNDER_REVIEW`, `APPROVED`, `REJECTED`, `CANCELLED`, `CLOSED`
- **ExcursionDisposition**: `PENDING`, `RELEASED`, `QUARANTINED`, `RETURNED`, `DESTROYED`
- **InvoiceMatchStatus**: `UNMATCHED`, `MATCHED`, `PRICE_VARIANCE`, `QUANTITY_VARIANCE`, `BOTH_VARIANCE`
- **NotificationChannel**: `IN_APP`, `EMAIL`, `SMS`, `TELEGRAM`, `WHATSAPP`, `PUSH`
- **NotificationSeverity**: `INFO`, `WARNING`, `CRITICAL`
- **PaymentMethod**: `CASH`, `CARD`, `MOBILE_MONEY`, `INSURANCE`, `BANK_TRANSFER`, `CREDIT`
- **PermissionAction**: `CREATE`, `READ`, `EDIT`, `DELETE`, `APPROVE`, `REJECT`, `CANCEL`, `PRINT`, `EXPORT`, `IMPORT`
- **PrescriptionStatus**: `NEW`, `UNDER_REVIEW`, `APPROVED`, `PARTIALLY_DISPENSED`, `DISPENSED`, `REJECTED`, `CANCELLED`
- **PurchaseOrderStatus**: `DRAFT`, `SUBMITTED`, `PROCUREMENT_REVIEW`, `FINANCE_REVIEW`, `APPROVED`, `ORDERED`, `PARTIALLY_RECEIVED`, `RECEIVED`, `CANCELLED`, `CLOSED`
- **QualityIncidentStatus**: `REPORTED`, `INVESTIGATING`, `ROOT_CAUSE_IDENTIFIED`, `CORRECTIVE_ACTION`, `PREVENTIVE_ACTION`, `VERIFICATION`, `CLOSED`
- **QualityIncidentType**: `DAMAGED_PRODUCT`, `TEMPERATURE_EXCURSION`, `SUSPECTED_COUNTERFEIT`, `SUPPLIER_QUALITY_ISSUE`, `INCORRECT_SHIPMENT`, `PACKAGING_DEFECT`, `RECALL`, `STORAGE_VIOLATION`
- **QuarantineReason**: `QUALITY_INVESTIGATION`, `DAMAGED_PACKAGING`, `TEMPERATURE_EXCURSION`, `SUSPECTED_COUNTERFEIT`, `RECALL`, `DOCUMENTATION_ISSUE`, `SHORT_SHELF_LIFE`, `REGULATORY_HOLD`
- **RecallSeverity**: `CLASS_I`, `CLASS_II`, `CLASS_III`
- **RecallStatus**: `OPEN`, `IN_PROGRESS`, `CLOSED`
- **RecallTaskStatus**: `PENDING`, `IN_PROGRESS`, `RECOVERED`, `NOT_RECOVERABLE`, `CLOSED`
- **ReturnDisposition**: `PENDING_INSPECTION`, `RESTOCK`, `QUARANTINE`, `RETURN_SUPPLIER`, `DESTROY`
- **ReturnType**: `CUSTOMER`, `SUPPLIER`, `BRANCH`
- **SaleStatus**: `DRAFT`, `HELD`, `COMPLETED`, `VOIDED`, `REFUNDED`, `PARTIALLY_REFUNDED`
- **StorageCondition**: `ROOM_TEMPERATURE`, `COOL`, `REFRIGERATED`, `FROZEN`, `CONTROLLED_ROOM_TEMPERATURE`
- **SupplierInvoiceStatus**: `DRAFT`, `SUBMITTED`, `MATCHED`, `DISPUTED`, `APPROVED`, `PARTIALLY_PAID`, `PAID`, `CANCELLED`
- **TransactionType**: `PURCHASE_RECEIPT`, `SALE`, `DISPENSING`, `TRANSFER_OUT`, `TRANSFER_IN`, `RETURN_IN`, `RETURN_OUT`, `ADJUSTMENT`, `DAMAGE`, `EXPIRY`, `RECALL`, `DISPOSAL`, `STOCK_COUNT`, `RESERVATION`, `RESERVATION_RELEASE`
- **TransferStatus**: `DRAFT`, `SUBMITTED`, `APPROVED`, `PICKING`, `DISPATCHED`, `IN_TRANSIT`, `PARTIALLY_RECEIVED`, `RECEIVED`, `COMPLETED`, `CANCELLED`
- **UserStatus**: `ACTIVE`, `SUSPENDED`, `LOCKED`, `DISABLED`
- **ValuationMethod**: `FIFO`, `WEIGHTED_AVERAGE`
